CLUSTER EXPERIMENT STATUS
Updated: 2026-08-26

SCHEMA V1 CUTOVER
- 94 reconstructable completed configurations are archived under
  archive/outputs/core/.../normalization/loss/run_n/seed_n, but they were
  produced by the superseded flat PatchTST implementation.
- The pinned source-adapted PatchTST/DLinear packages are now the sole current
  backbones. The migrated runs remain historical evidence and must not be
  selected for a current comparison.
- Their original trees remain under
  archive/outputs/archive/legacy_pre_schema_v1_2026-08-07/ for audit only.
- Reports now belong under outputs/reports/<family>/<mode>.
- Runtime roots default to logs/ and outputs/ and are overridable. Each of the
  four DGX fronts has a scientific-equivalent Selena front selecting
  logs_selena/ and outputs_selena/.
- Active `outputs/` and `logs/` contain only their placeholders. Synchronized
  historical results were moved to the ignored project archive on 2026-08-25,
  and the ten historical Slurm log files were removed on 2026-08-26.

LATEST COMPLETED JOB
Job ID: 42315
Run/profile: EXPERIMENT_MODE=small sbatch revin.slurm (core family)
State: completed successfully on 2026-08-05 at 11:09:07 +02:00
Artifacts synchronized locally: archived locally; no active output/log payload
Analyzed locally: yes
Analysis note: All 90/90 configurations and 270/270 seed runs are complete
under the superseded backbone implementation.
Final stderr is empty; all synchronized metrics are finite; artifact, count,
workflow-marker, table, and lightweight test checks pass. See
the archived legacy analysis record. The corrected
protocol does not numerically reproduce the ECML table: it preserves the strong
Electricity result and affine-layer redundancy, but not Traffic's preference
for standard normalization or uniformly better normalized backpropagation.

CURRENT OR NEXT CLUSTER JOBS
No RevIN job is currently running.

1. Run `RUN_CONFLICT_POLICY=new EXPERIMENT_MODE=test sbatch revin.slurm` and
   inspect all four new current-package smoke configurations.
2. Run the equivalent `revin_selena.slurm` test and validate isolated Selena
   artifacts plus both synchronization directions.
3. Rerun the complete desired PatchTST core grid with the new-run policy before
   using any normalization conclusion.
4. Run the remaining nMSE, exotic, MIN, and DLinear families only under the
   pinned packages; no superseded manifest may satisfy those comparisons.

No Selena front or transfer has yet been exercised on the clusters.

The current protocol intentionally uses one run seed for the user partition,
data/window sampling, initialization, and optimization, so seed standard
deviations combine all stochastic sources. Update this handoff after each
submission, synchronization, and analysis.
